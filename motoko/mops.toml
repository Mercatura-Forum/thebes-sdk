[package]
name = "thebes-lib"
version = "0.1.0"
description = "Thebes Protocol Motoko backend lib — Admin (controller-gated ops), MemphisAuth (passkey session verification), Users (roles/profiles/avatars), Pagination (bounded list reads). Shared by the Thebes example dapps."
repository = "https://github.com/Mercatura-Forum/thebes-sdk"
keywords = ["thebes", "egypt-l1", "motoko", "auth", "admin"]
license = "Apache-2.0"

[dependencies]
core = "2.5.0"
